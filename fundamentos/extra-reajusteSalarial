#include <stdio.h>

int main() {
    float salarioMensal, reajuste, novoSalario;
    scanf("%f %f", &salarioMensal, &reajuste);
    reajuste = reajuste/100;
    novoSalario = salarioMensal * reajuste + salarioMensal;
    printf("%f", novoSalario);
    return 0;
}